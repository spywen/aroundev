package com.around.dev.tests.controller;

/**
 * Created by laurent on 03/11/14.
 */
/*
@RunWith(SpringJUnit4ClassRunner.class)
@WebAppConfiguration
@ContextConfiguration(classes = {WebAppConfigs.class, AppConfigs.class, BusinessConfigs.class}, loader = AnnotationConfigContextLoader.class)
public class ControllerTests {
*/
    //region init testing
    //endregion

/*
    private MockMvc mockMvc;
    static UserConnectedProfile admin = new UserConnectedProfile("lolo", null);

    @Mock
    UserBusiness userBusiness;
    @Mock
    UserRepository userRepository;

    @InjectMocks
    private GController gController;


    @Before
    public void setup() {
        MockitoAnnotations.initMocks(this);

        this.mockMvc = MockMvcBuilders.standaloneSetup(gController).build();
    }

    @Test
    public void simple() throws Exception {
        this.mockMvc.perform(get("/"))
                .andExpect(status().isOk());
    }
*/

    /*@Test
    public void SinglePageApplicationTest(){
        GController c = new GController();
        ModelAndView mav= c.SingleApplicationPage();
        Assert.assertEquals("index", mav.getViewName());
    }*/

    /*@Test
    public void SinglePageApplication_Arguments_Test(){
        GController c = new GController();
        ModelAndView mav= c.SingleApplicationPage();
        Assert.assertEquals("YEAH", mav.getModel().get("userConnected"));
    }*/
//}
